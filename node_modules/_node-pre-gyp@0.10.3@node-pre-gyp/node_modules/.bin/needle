#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_needle@2.2.4@needle/bin/needle" "$@"
  ret=$?
else 
  node  "$basedir/../../../_needle@2.2.4@needle/bin/needle" "$@"
  ret=$?
fi
exit $ret
